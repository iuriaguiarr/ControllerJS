import styled from "styled-components";

const Welcome = styled.p`
  font-family: "Aldo";
  font-size: 2rem;
  color: var(--white);

  font-weight: lighter;
  text-align: center;
  margin: 0 4rem;
  margin-bottom: 8rem;
`;

export default Welcome;
